{
  "name": "audio-transcription-azure",
  "version": "1.0.0",
  "description": "Audio transcription with Azure Speech to Text and speaker diarization",
  "author": "Philippe Savary",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd src/backend && npm run dev",
    "start:frontend": "cd src/frontend && npm run dev",
    "install:all": "npm install && cd src/backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
} 